{
  "name": "tpc6",
  "version": "1.0.0",
  "description": "Task Handler",
  "main": "tasks.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev" : "nodemon --verbose --ignore 'db.json' --ignore './.node_modules/*' --ignore './.history/*' tasks.js",
    "go" : "node tasks.js"
  },
  "author": "hc",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0"
  }
}
